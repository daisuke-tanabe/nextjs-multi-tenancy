{
  "name": "multi-tenancy",
  "version": "0.1.0",
  "private": true,
  "engines": {
    "node": "20.x"
  },
  "packageManager": "npm@10.2.4",
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "build": "turbo build",
    "dev": "turbo dev",
    "format": "prettier --write \"**/*.{js,ts,tsx,md}\"",
    "lint": "turbo lint"
  },
  "devDependencies": {
    "prettier": "^3.2.4",
    "turbo": "^1.11.3"
  }
}
